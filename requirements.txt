# Core inference
# NOTE: vLLM/torch versions track the host's CUDA driver. The defaults below
# target CUDA 12.4 drivers (nvidia-smi "CUDA Version: 12.4"). If your driver
# supports 12.6/12.8, you can bump these. See docs/SETUP.md §"驱动 / CUDA 不匹配".
vllm==0.8.5
torch==2.6.0
torchvision==0.21.0
# vLLM 0.8.5 is built against transformers 4.5x; 4.52+ and 5.x break model import.
transformers==4.51.3
tokenizers>=0.21,<0.22
mistral_common==1.5.4
accelerate>=0.34.0

# Multimodal data
Pillow>=10.0.0
datasets>=2.20.0

# Answer extraction
sentence-transformers>=3.0.0
scikit-learn>=1.3.0

# Utilities
pyyaml>=6.0
tqdm>=4.66.0
numpy>=1.26.0
pandas>=2.0.0
